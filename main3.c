#include <stdio.h>

int main(){

    // arithmetic operators = + - * / % ++ --

    int x = 10;
    int y = 2;
    int z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;
    // z = x % y;

    // x++;
    // x--;

    // x = x + 2;
    // x+=3;
    // x-=3;
    // x *= 2;
    x /= 2;

    printf("%d", x);

    return 0;

}